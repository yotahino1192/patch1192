# Public values only. Copy to mobile/.env.production.local and fill actual values.
# Do not source the server environment into the mobile build shell.
PATCH_ENV=production
PATCH_API_URL=
PATCH_CLERK_PUBLISHABLE_KEY=
PATCH_CLERK_ISSUER=
# Values must match the server and config/release-policy.json.
# npm run ios:sync (never ios:sync:local for an Archive candidate).
