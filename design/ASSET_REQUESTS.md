# Patch artwork refinements

Layout and functionality are implemented. No generated substitute was used. The repository and all eight images embedded in the original handoff were inspected; the handoff contains full screens, not separate pose masters.

| Reference | Requested asset and placement | Size / format | Description and current limitation |
| --- | --- | --- | --- |
| `home-empty-reference.png` | Standing Patch mascot, early Home | ~240×250 CSS px; transparent PNG at 2–3×, or original SVG; transparent background required | Mint body, peach face panels, brown outlines, standing pose and mint ground shadow. Current original `loop-companion.jpeg` has a white sticker border/shadow and slightly different proportions; it is reused instead of redrawing an existing mascot. A clean approved master would match more closely. |
| `home-active-normal.png`, `lesson-preview.png` | Reading mascot, Home and preview | ~116×110 CSS px; transparent PNG at 3×, or original SVG; transparent background required | Same Patch character holding an open green book, accent rays and mint ground shadow. Current approved-reference crop matches the pose, but the source's paper texture and small edge remnants limit transparency on non-white backgrounds and enlargement. |
| `lesson-complete.png` | Celebrating mascot, completion | ~310×280 CSS px; transparent PNG at 2–3×, or original SVG; transparent background required | Raised arms, jumping legs, colorful confetti and ground shadow. Current approved-reference crop preserves the artwork but retains some source paper texture; a standalone master would give clean edges at all scales. |
