# Project status

最終確認: 2026-09-12。実装構成は [ARCHITECTURE.md](ARCHITECTURE.md)。以下はローカル作業ツリーを確認した状態であり、本番反映済みという意味ではない。

## 基盤改善（前フェーズ）

| 対象 | 以前 | 現在 |
| --- | --- | --- |
| 学習評価の再送 | 応答消失・再送で同じ回答が重複する余地 | 送信前に操作IDを保存。DB制約・トランザクションで同じ回答は一度だけ記録 |
| 同時操作 | 古いカード状態から追加評価できる余地 | 期待カウントが違えば409で再読込・照合 |
| 再開 | 履歴表示の500件制限が復元に影響 | セッション履歴を別取得。完了カードや取り消しの復元が表示上限に依存しない |
| 失敗後の誤答復元 | 順番・誤答回数がずれる余地 | 記録済み操作を照合して一度だけ反映 |
| 入力 | JSON形式・本文量などの境界が不十分 | 共通の2MiB上限、400/413/415、評価・教材・カード検証を追加 |
| Node 22 | PDFのmodern buildがPromise.try未対応で失敗 | 本体・Workerを公式legacy buildへ統一 |
| 品質 | チェック入口が分散、生成WorkerがLintを埋める | `npm run check`、Node 22 CI、外部生成物をLintから除外 |

基盤改善フェーズでは既存の画面構成・アイコン・採点方式を維持した。今回の初回モードの差分は下記参照。

## 依存関係（前フェーズの記録）

- Next.js / eslint-config-next: 16.2.6 → **16.3.4**。監査が示した修正版へ揃えた。
- baseline-browser-mapping: **2.11.22**（互換範囲内更新）。
- DB通信配下のws: **8.21.3**。旧Cloudflare開発依存の8.18.0固定と切り離すため、`@libsql/isomorphic-ws` 配下だけ `^8.21.0` をoverride。上流が安全版に揃ったらoverrideを再評価する。
- `npm audit fix --force` は使用していない。

Next.jsの[AVIF画像最適化の脆弱性](https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4)は16.3.3以降が修正対象。本アプリは主に通常のimgとローカル素材を使い、攻撃成立は再現していないが、フレームワークの画像処理経路を含め更新した。wsも[通信によるメモリー枯渇の指摘](https://github.com/advisories/GHSA-96hv-2xvq-fx4p)の対象版がDB依存に入っていたため更新した。

本番依存の `npm audit --omit=dev` は **0件**。全依存の監査は **19件（高14・中4・低1）** が残る。主にCloudflare/Vite/vinext系、開発ツール配下、Drizzle Kit等。現在のNext.js実行経路とは分けて扱う。開発ツールも安全という意味ではなく、旧開発サーバーを外部公開しない。強制更新はvinextのメジャー変更やDrizzle Kitのダウングレードを含むため今回は実施していない。

## 検証結果（前フェーズの記録）

Node **22.23.2** で確認。

| チェック | 結果 |
| --- | --- |
| `npm run typecheck` | 成功 |
| `npm run lint` | エラー0、警告42。未使用変数・通常img等が残る |
| `npm run test:unit` | 66件すべて成功 |
| `npm run build` | production build成功 |
| `npm run check` | 上記をまとめて成功 |
| 本番ビルドのHTTPスモーク | ホーム、データ取得、教材保存、評価、再送、取り消し、セッション復元、PDF Worker配信、AI未設定時503を確認 |

スモークは一時DB・AIキー無効の隔離環境で実施。テストには「DBコミット後に応答生成だけ失敗→API再送」の実経路検証と、DB制約を直接破ろうとする検証を含む。

未検証: Tursoリモート接続での負荷・障害、本番マイグレーション時間、実際の有料AI応答、実機ブラウザー操作の網羅、GitHub上の新規CI実行。フォント取得が制限された環境ではビルドに失敗するため、ネットワーク許可のある環境で成功を確認した。

## 公開前の判断事項（未実装）

| 項目 | 現在の問題 | 推奨案と理由 | 実装規模・優先度 |
| --- | --- | --- | --- |
| アクセス制御 | APIにログイン検証がない。全員loop-owner | 限定試用ならホスト側のアクセス保護を画面・API・Preview/Productionに適用し、未認証で遮断されることを確認。現構成のまま閉じられる | 小。一般公開より先。実際のホスト設定は未確認 |
| ユーザー認証 | 利用者別データ分離が実効的でない | 公開版は標準OIDC対応の認証サービスとサーバー検証セッションを推奨。検証済み安定IDを全APIのuser_idに使用。既存所有者条件を活かせる | 中〜大。サービス選定・既存loop-ownerデータの帰属を決めてから。今回決定・移行なし |
| AI利用制限 | 呼び出し回数、同時実行、費用を制限していない | サーバーでユーザー／IP別制限、共有DB/Redisで回数・同時実行枠を管理。AI側予算も併用。プロセス内カウンターだけでは複数インスタンスを防げない | 中。認証方針と合わせ公開前に実施 |
| 入力検証 | 今回は主要境界のみ。管理・AIオプション等に文字列変換が残る | 全アクションの共有スキーマと拒否テストを段階追加。UI側の制限をAPIでも保証する | 小〜中。公開前に補完 |

## 意図的に見送ったもの

- 全件取得の最適化、資料／カードモデルの全面変更、外部キー導入：今回の不整合は限定した追加索引と処理で解決できた。データ量と利用者設計が決まってから進める。
- page.tsx分割、CSS整理、未使用コード大量削除、画像最適化：今回の高リスク処理と無関係な変更を広げない。
- IndexedDB移行：保存失敗通知は維持。下書きの容量・複数端末同期要件が明確になってから。
- 既存の重複履歴の修復：どの記録が正しいか自動判定できない。既存データを削除・移送していない。
- 教材保存・AI会話の再送冪等化：今回の保証は学習評価のみ。重要な次候補。
- 旧Cloudflare系の依存更新／削除：廃止方針を確認して別変更にする。開発依存の監査指摘を本番0件に含めて隠さない。

## 次の順序

1. 公開範囲とアクセス保護を確認し、認証・既存データ帰属・AI利用枠を設計する。
2. 全API入力検証を補完し、必要なら教材保存・会話の再送にも操作ID方式を適用する。
3. 旧開発基盤を整理し、残存監査指摘とLint警告を範囲を分けて解消する。

配備時の注意: 0005・0006は初回DBアクセスで適用される追加変更。旧画面は新評価APIの必須パラメーターを持たないため再読込が必要。本番適用・デプロイ・pushは今回実施していない。


## オンボーディング実装（2026-09-11）

名前、興味3つ、学習目的1つから重複のない3セットを提示し、選択時に自動保存する。18セット148枚の実カードを用意した。初回は既存Studyで3枚だけ回答し、誤答でも次へ進む。DBで3枚の履歴を確認してDay1を記録し、完了画面からホームへ進む。新しいログイン方式や別の復習アルゴリズムは追加していない。

**制約**: 本アプリは現在も認証なし・単一所有者。Google/Appleの名前の自動取得や本当の再ログインは未検証・未実装。既存所有者に教材等があれば案内を出さない。複数ユーザーに別々の初回体験を提供するには、先に認証と `requestUserId` の接続が必要。既存利用者のDBをリセットして案内を出す操作は実施していない。

DBの0006は新規テーブル追加のみ。本番には未適用。プロフィール・完了状態はTurso/libSQLの保存経路に保持し、localStorageだけには依存しない。3枚への回答後に閉じた場合も達成画面を復元する。

### 確認

- Node 22: 型検査、66件のテスト、production build成功。Lintはエラー0・既存警告42。
- 実ブラウザー: 名前入力、興味が3つ未満の無効化、4つ目の選択防止、解除、目的選択、おすすめ3件、自動保存を確認。
- 保存コミット後の通信失敗を再現して再選択しても、セットは1つだけ。
- 1枚回答後にlocalStorageを消して再読み込みしても、DBから2枚目を再開。
- 誤答1枚＋正答2枚で完了。ストリーク1日、記録3件、ホーム移動、再読み込み時に初回案内が出ないことを確認。
- 既存データ保持、途中のDB失敗のロールバック、日付をまたぐ3枚学習はDBテストで確認。
- 興味、おすすめ、学習、達成画面を320/390/768pxで横はみ出しなく確認。

### 変更ファイル

- 画面: `app/onboarding.tsx`（新規）、`app/page.tsx`、`app/globals.css`、`lib/translations.ts`
- 教材と状態: `lib/onboarding.ts`（新規）、`lib/types.ts`、`lib/workspace.ts`
- DB/API: `db/schema.ts`、`db/store.ts`、`app/api/data/route.ts`、`drizzle/0006_broad_boomerang.sql` とmeta
- 検証: `tests/onboarding.test.mjs`（新規）、`scripts/check-onboarding.mjs`（新規）、`package.json`、既存DBテスト3ファイル
- 文書: `ARCHITECTURE.md`、`STATUS.md`

教材の投資用語は [Investor.govの投資入門](https://www.investor.gov/introduction-investing)、スピノザの基本概念は [Stanford Encyclopedia of Philosophy](https://plato.stanford.edu/entries/spinoza-attributes/) 等と照合。プリセットは独自に書いた日本語の入門用要約で、個別の投資助言や資格試験範囲の保証ではない。

## iOS / Capacitor Phase 1（2026-09-12）

### 今回の変更

- Capacitor core/ios/cli **8.5.2** とSwift Package Managerを使用するXcode Appターゲットを追加。iOS **16.4以上**。現時点の開発用Bundle IDは `com.patch.learning`。
- `mobile/` のVite入口で既存 `app/page.tsx` と全コンポーネント・学習ロジックを再利用。UIのコピーやSwiftUI/React Nativeへの書き換えなし。
- Webの同一オリジン通信は維持。iOSのみ共通APIクライアントからネイティブHTTPで設定済みバックエンドへ通信。APIの認証/CORS/DB処理は変更なし。
- フォント・画像・PDF Worker/cmapsを同梱。モバイルの実行時Google Fonts依存なし。
- `PATCH_API_URL` はモバイル専用envまたはシェルで指定。本番ビルドはHTTPS設定必須。Xcode Releaseでは開発用bundleを拒否する。
- Viteを既存8.0.13から **8.0.16** へ最小パッチ更新。今回モバイル開発に使用するため、監査が報告したWindowsの開発サーバー関連指摘へ対応した。
- スクリプトと環境別起動手順は [mobile/README.md](mobile/README.md)。生成したWeb bundleはGitに含めず、実行前にsyncする。

### 確認済み

Node **22.23.2** / Xcode **26.1.1** / iPhone 17 Pro Simulator **iOS 26.1**。

| チェック | 結果 |
| --- | --- |
| `npm run typecheck` | 成功 |
| `npm run lint` | エラー0、既存警告42 |
| `npm run test:unit` | 70件成功（共通APIクライアントの4件を追加） |
| `npm run build` | Next.js production build成功 |
| `npm run mobile:build` | HTTPSの検証用originを指定して成功。本番Vercelへの接続検証ではない |
| `npm run ios:sync:local` | 静的bundle生成・ネイティブプロジェクト同期成功 |
| Xcode Debug Simulator build | 成功。署名不要のSimulatorビルド |
| Xcodeプロジェクトを開く | 成功 |
| Simulatorへinstall/launch | 成功。日本語フォント付き初回画面を表示 |
| Simulator → 既存Next API | 隔離した一時DBのバックエンドで初回取得、名前・興味の保存、目的選択画面への遷移を確認 |
| WKWebView localStorage | テストアプリ領域に `loop-workspace-v1` の保存を確認 |
| Releaseの誤設定防止 | 同期済みローカル開発bundleを拒否することを確認 |
| 既存Webのブラウザーテスト | 初回案内、保存応答消失、DB復元、3枚学習、Day1、ホーム、再読込、レスポンシブ幅を確認 |

Simulatorの接続先は一時DB `/tmp/patch-ios-phase1.db` を使用したローカルバックエンド。AIキーは無効。既存のローカルDB・本番DBのデータを移行・削除していない。

### 残る確認・制約

- iOSの全オンボーディング完了、学習/復習の全操作、長い画面のスクロール、キーボード、PDF/Office取り込み、オフライン復元は実機を含む手動確認が必要。SimulatorのUI自動操作では全フローを通していない。Webテストの成功をiOS全機能の検証と混同しない。
- 本番Vercel/Turso接続、保護されたVercel環境へのアクセス、実際のAI生成、iPhone実機、署名付きArchive、TestFlight/App Store提出は未検証。
- アプリのアイコン/起動画面はCapacitorの生成テンプレート。公開前に最終素材へ変更する。
- bundleは約50MB。日本語フォントや既存画像を同梱した結果であり、今回全面最適化は行っていない。
- native API transportは現在のJSON APIに限定。ファイルの直接アップロード、ストリーミング、AbortSignal連携は対象外。
- localStorageは端末内のみ。アプリ削除/データ消去で下書きは失われ、Web版や別端末との同期はしない。
- 認証、loop-owner移行、通知、ウィジェット、ディープリンクは未実装。共有所有者のままなので一般公開には使わない。
- インストール後の全依存監査は21件（高11・中9・低1）。既存の開発依存指摘に加えCapacitor CLI→xcode→uuidの指摘がある。確認したxcode呼び出しはuuid.v4で、報告対象のv3/v5/v6＋buffer経路ではない。強制ダウングレード/メジャーoverrideはせず、上流修正を追跡する。前フェーズの監査件数は当時の記録として残している。

次は、このシェルでの実機スモーク確認を完了し、認証・ユーザー別データ分離のPhase 2へ進む。正式な認証サービスと既存データの帰属は実装前に決める。
